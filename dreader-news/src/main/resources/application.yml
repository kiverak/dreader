spring:
  application:
    name: dreader-news
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: dreader-micro, dreader-news
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/dreaderapp-realm