spring:
  application:
    name: dreader-news-parser
  datasource:
    url: jdbc:postgresql://localhost:5435/dreader_news_parser
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

server:
  port: 8081

logging:
  level:
    root: INFO