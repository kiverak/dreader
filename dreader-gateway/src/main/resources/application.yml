spring:
  application:
    name: dreader-gateway
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: dreader-gateway

# for testing access with jwt token
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: http://localhost:8180/realms/dreaderapp-realm